# Redirect non-www to www (canonical domain)
https://tsfinanse.com/* https://www.tsfinanse.com/:splat 301!

# Netlify serves static files first (prerendered HTML),
# then falls back to SPA index.html for client-side routing.
# This 200 rewrite only fires when no matching static file exists.
/*  /index.html  200
