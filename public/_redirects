# www redirect handled by Netlify domain settings (not here - causes loop)
# Netlify serves static files first (prerendered HTML).
# Unknown URLs must 404, not fall back to SPA index.html, to avoid soft-404 indexing noise.

# Force static .well-known files (Netlify reads from public/.well-known/)
# MCP Server Card intentionally omitted: TS Finanse does not expose a public API. Default 404 is correct.
/.well-known/api-catalog            /.well-known/api-catalog            200
/.well-known/agent-skills/index.json /.well-known/agent-skills/index.json 200

/kontakt  /#contact  301
